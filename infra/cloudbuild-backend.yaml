# Cloud Build configuration for Backend (FastAPI)
steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/city-platform-backend:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/city-platform-backend:latest'
      - './backend'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/city-platform-backend:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/city-platform-backend:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'city-platform-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/city-platform-backend:$SHORT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8000'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'

# Substitutions
substitutions:
  _REGION: 'europe-west1'  # Change to your preferred region

# Images to be pushed to the Container Registry
images:
  - 'gcr.io/$PROJECT_ID/city-platform-backend:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/city-platform-backend:latest'

# Build options
options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'

