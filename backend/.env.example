# Firebase Admin SDK Configuration
# Option 1: Individual credentials (from Firebase Console > Project Settings > Service Accounts)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY_ID=your-private-key-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your-client-id

# Option 2: Service Account JSON file path (alternative to individual credentials above)
# If using this option, comment out the individual credentials above
# FIREBASE_SERVICE_ACCOUNT_PATH=path/to/serviceAccountKey.json

# Firebase OAuth URIs (usually don't need to change)
FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token

# API Settings
# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.run.app

# Environment
# Options: development, staging, production
ENVIRONMENT=development
